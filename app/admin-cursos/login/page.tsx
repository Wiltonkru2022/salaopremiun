import Link from "next/link";
import { entrarCursos } from "@/app/cursos/actions";
export default async function AdminLogin({searchParams}:{searchParams:Promise<{erro?:string}>}){const {erro}=await searchParams;return <main className="cursos-main"><div className="cursos-wrap cursos-form-shell" style={{maxWidth:520}}>{erro&&<p className="cursos-alert">{erro}</p>}<form className="cursos-form" action={entrarCursos}><span className="cursos-eyebrow" style={{background:"#f2e8ef",color:"#6d235f"}}>Acesso restrito</span><h1 style={{marginTop:14}}>Portal da professora</h1><p className="cursos-muted">Entre com o e-mail administrativo autorizado.</p><div className="cursos-form-section"><div className="cursos-field"><label htmlFor="email">E-mail</label><input id="email" name="email" type="email" required/></div><div className="cursos-field" style={{marginTop:14}}><label htmlFor="password">Senha</label><input id="password" name="password" type="password" required/></div></div><button className="cursos-button" style={{width:"100%",marginTop:22}}>Entrar na administração</button><p className="cursos-muted" style={{textAlign:"center"}}>Primeiro acesso? <Link href="/cursos/cadastro" style={{color:"#6d235f",fontWeight:900}}>Crie sua conta administrativa</Link> usando o e-mail autorizado.</p></form></div></main>}
