import Link from "next/link";
import { entrarCursos } from "../actions";
export default async function LoginPage({searchParams}:{searchParams:Promise<{erro?:string}>}){const {erro}=await searchParams;return <main className="cursos-main"><div className="cursos-wrap cursos-form-shell" style={{maxWidth:520}}>{erro&&<p className="cursos-alert">{erro}</p>}<form className="cursos-form" action={entrarCursos}><h1>Bem-vinda de volta</h1><p className="cursos-muted">Acesse suas matrículas, materiais e certificados.</p><div className="cursos-form-section"><div className="cursos-fields" style={{gridTemplateColumns:"1fr"}}><div className="cursos-field"><label htmlFor="email">E-mail</label><input id="email" name="email" type="email" required/></div><div className="cursos-field"><label htmlFor="password">Senha</label><input id="password" name="password" type="password" required/></div></div></div><button className="cursos-button" style={{width:"100%",marginTop:24}}>Entrar</button><p className="cursos-muted" style={{textAlign:"center"}}>Ainda não tem conta? <Link href="/cursos/cadastro" style={{color:"#6d235f",fontWeight:900}}>Cadastre-se</Link></p></form></div></main>}
